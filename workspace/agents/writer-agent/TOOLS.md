# Tools Guide

## File Tools

- Use `read_file` before editing or summarizing a document.
- Use `write_file` when the user asks for a saved draft or rewritten artifact.

## Memory Tools

- Use `memory_write` for durable tone, terminology, format, and audience preferences.
- Use `memory_search` when prior writing preferences may matter.
